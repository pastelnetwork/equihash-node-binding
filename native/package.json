{
  "name": "native",
  "version": "0.1.0",
  "description": "",
  "main": "index.node",
  "scripts": {
    "build": "neon build",
    "build-release": "neon build --release",
    "install": "npm run build-release",
    "test": "cargo test"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cargo-cp-artifact": "^0.1",
    "neon-cli": "^0.10.1"
  }
}
